import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import CreateContact from './App';

// it('renders without crashing', () => {
//   const div = document.createElement('div');
//   ReactDOM.render(<App />, div);
// });

it('adds 2 with 3 and equals 5', () => {
  expect(2 + 3).toBe(5);
});

